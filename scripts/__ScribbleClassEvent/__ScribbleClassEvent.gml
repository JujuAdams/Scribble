// Feather disable all

/// @param name
/// @param data

function __ScribbleClassEvent(_name, _data) constructor
{
    //These are publicly exposed via `.get_events()`
    name        = _name;
    data        = _data;
    revealIndex = undefined;
    //TODO - Restore character index
}
