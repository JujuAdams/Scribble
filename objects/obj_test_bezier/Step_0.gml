if (keyboard_check(ord("1"))) { x0 = lerp(x0, mouse_x, 0.1); y0 = lerp(y0, mouse_y, 0.1); }
if (keyboard_check(ord("2"))) { x1 = lerp(x1, mouse_x, 0.1); y1 = lerp(y1, mouse_y, 0.1); }
if (keyboard_check(ord("3"))) { x2 = lerp(x2, mouse_x, 0.1); y2 = lerp(y2, mouse_y, 0.1); }
if (keyboard_check(ord("4"))) { x3 = lerp(x3, mouse_x, 0.1); y3 = lerp(y3, mouse_y, 0.1); }