typist = scribble_typist_legacy();
typist.TypeIn(1, 0);

element = scribble("not sure if this is helpful, but attemtping a move from scribble 7.1 to the newest, i'm getting a consistent crash with typist.GetTypeState() replacing what was a typewriter_get_state().")
          .layout_wrap(400)
          .align(fa_center, fa_middle);